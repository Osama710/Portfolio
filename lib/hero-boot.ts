/** Inline boot script — runs on DOMContentLoaded, before the React bundle hydrates. */
export const heroBootScript = `(function(){if(window.matchMedia("(prefers-reduced-motion: reduce)").matches){document.querySelectorAll(".hero-count").forEach(function(el){var s=el.dataset.suffix||"";el.textContent=(el.dataset.value||"0")+s;el.dataset.done="1";});return;}function ease(p){return 1-Math.pow(1-p,3);}function animate(el){if(el.dataset.done)return;el.dataset.done="1";var target=+el.dataset.value||0;var suffix=el.dataset.suffix||"";var dur=+(el.dataset.duration||700);var t0=performance.now();el.textContent="0"+suffix;function tick(now){var p=Math.min((now-t0)/dur,1);var v=Math.round(ease(p)*target);el.textContent=v+suffix;if(p<1)requestAnimationFrame(tick);}requestAnimationFrame(tick);}function boot(){document.querySelectorAll(".hero-count").forEach(animate);document.documentElement.classList.add("hero-booted");}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot);else boot();})();`;
